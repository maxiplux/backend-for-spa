####
## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring:
  datasource:
    url: jdbc:postgresql://${PGHOST:LOCALHOST}:${PGPORT:5432}/${PGDATABASE:postgres}
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:postgres}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    rest:
      detection-strategy: annotated
  main:
    allow-circular-references: true


  #server.servlet.context-path=/app
  security:
    oauth2:
      client:
        registration:
          pingone:
            client-id: d341b92f-6c2c-42b9-ba5a-4edd1698b8a4
            client-secret: zkUBuHs7xeGJ2wE1sX71S_IQu9nHI05il4BFohtGU4a1oWwanddsOX_YItIVOQKd
            scope:
              - email
              - openid
              - phone
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Ping One
        provider:
          pingone:
            issuer-uri: https://auth.pingone.com/21620bf7-6daf-4fd6-a951-ae0f9b1bd7af/as
            user-name-attribute: username
            #https://login.microsoftonline.com/051b2f62-41c5-46d7-9949-540d1a7c9c0e/v2.0/.well-known/openid-configuration
            #https://learn.microsoft.com/en-us/entra/identity-platform/v2-protocols-oidc
#            authorization-uri: https://login.microsoftonline.com/{tenantId}/oauth2/v2.0/authorize
#            token-uri: https://login.microsoftonline.com/{tenantId}/oauth2/v2.0/token
#            user-info-uri: https://graph.microsoft.com/oidc/userinfo






server:
  servlet:
    context-path: /app
  port: 8080

logging:
  level:
    root: INFO
    org:
      springframework:
        web: DEBUG
        security:
          web: debug
          saml: DEBUG
      opensaml:
        xmlsec: DEBUG
  org.opensaml.xmlsec: DEBUG
#openssl req -x509 -newkey rsa:2048 -keyout private.key -out certificate.crt -days 365 -nodes -subj "/CN=spring-boot"



